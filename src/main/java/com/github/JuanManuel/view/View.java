package com.github.JuanManuel.view;

import javafx.scene.Parent;

public class View extends Parent {
    public Parent scene;        // La escena que contiene los elementos de la interfaz.
    public Controller controller; // El controlador que maneja la lógica de la vista.
}
